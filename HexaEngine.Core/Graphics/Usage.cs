namespace HexaEngine.Core.Graphics
{
    public enum Usage
    {
        Default = unchecked(0),
        Immutable = unchecked(1),
        Dynamic = unchecked(2),
        Staging = unchecked(3)
    }
}